<li>
	{{sushi_name}}

	<button class="devouredSushi" data-id="{{this.id}}" data-newSushi="{{devoured}}" > 
		{{#if devoured}}Eat!{{else}}Devour{{/if}}
	
	</button>

	<button class="deleteBtn" data-id="{{this.id}}">DELETE</button>

	{{!-- <button class="devourRoll" data-id="{{this.id}}" data-newSushi="{{devoured}}">DEVOURED!</button> --}}
</li>
